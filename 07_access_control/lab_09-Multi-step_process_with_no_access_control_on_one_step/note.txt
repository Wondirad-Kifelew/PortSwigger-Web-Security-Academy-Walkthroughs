Multi-step process with no access control on one step

here the admin changes the role of the user in 2 steps it clicks the upgrade then a confirmation page comes, only then will it change the role of hte user. so check this processes in burp by sending the first click to 'upgrade' button then send that to the repeater, next when clicking the confirmation button 'yes' button send a separate request to the repeater and now you observe that escalating to admin can be done in 1 step since after confirmation the payload looks like this "action=upgrade&confirmed=true&username=wiener" and the request looks like "POST /admin-roles HTTP/2" so use this to escalate to admin in the user account page. simpley refresh, capture that in the repeater change teh request and the payload accordingly to escalate.
